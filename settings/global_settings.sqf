/* Contains big overall settings, such as enabling missions setup */

// If set to 1, mission setup will run instead. Set to 0 to run the mission normally
/* NOTE: Currently mission setup must be run on a client server (not dedicated) for things (mostly debug functions) to work properly. 
If running the mission normally, then a dedicated server can be used. */
#define ENABLE_MISSION_SETUP 1